# 🔧 cronジョブ設定の修正手順

**問題**: cronファイルへの権限エラー

---

## 解決方法

### ステップ1: 正しいグループ名を確認

```bash
# 現在のユーザーのグループを確認
groups

# または
id

# 出力例:
# uid=1000(AdminUser) gid=1000(admin) groups=1000(admin),27(sudo)
```

### ステップ2: 正しいグループ名で権限を設定

```bash
# 正しいグループ名（通常は 'admin'）を使用
sudo chown AdminUser:admin /var/spool/cron/crontabs/AdminUser
sudo chmod 600 /var/spool/cron/crontabs/AdminUser
```

---

**作成日**: 2025-11-02  
**作成者**: AI Assistant

