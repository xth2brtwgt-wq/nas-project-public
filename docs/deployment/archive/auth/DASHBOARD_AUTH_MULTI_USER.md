# 👥 ダッシュボード認証統合 複数ユーザー対応

**作成日**: 2025-11-04  
**目的**: 複数ユーザー対応の難易度と実装方法を検討

---

## 🔍 複数ユーザー対応の難易度

### 難易度評価: ⭐⭐☆☆☆（やや簡単）

**理由**:
- 基本的な機能は既存の計画に追加するだけで実装可能
- データベース設計がシンプル
- セッション管理機能は既に計画しているので、追加コストは小さい

---

## 📋 実装内容の変更点

### 変更が必要な部分

#### 1. データベース設計

**単一ユーザー（元の計画）**:
- セッションテーブルのみ
- ユーザー情報は環境変数から読み込み

**複数ユーザー対応**:
- **ユーザーテーブル**を追加
  - `id`: ユーザーID（主キー）
  - `username`: ユーザー名（一意）
  - `password_hash`: パスワードハッシュ（bcrypt）
  - `created_at`: 作成日時
  - `updated_at`: 更新日時
  - `is_active`: 有効/無効フラグ

- **セッションテーブル**（既存の計画）
  - `session_id`: セッションID（主キー）
  - `user_id`: ユーザーID（外部キー）
  - `created_at`: 作成日時
  - `expires_at`: 有効期限

#### 2. ログイン機能

**単一ユーザー（元の計画）**:
```python
# 環境変数からユーザー名とパスワードを読み込み
username = os.getenv('DASHBOARD_USERNAME')
password = os.getenv('DASHBOARD_PASSWORD')
```

**複数ユーザー対応**:
```python
# データベースからユーザーを検索
user = get_user_by_username(username)
if user and verify_password(password, user.password_hash):
    # ログイン成功
    create_session(user.id)
```

#### 3. パスワード管理

**必要な機能**:
- パスワードハッシュ化（bcrypt）
- パスワード検証

**追加の依存関係**:
```python
# requirements.txt
bcrypt==4.0.1
```

---

## 🎯 実装方針（複数ユーザー対応版）

### フェーズ1: 基本機能（必須）

1. **データベース設計**
   - ユーザーテーブルを作成
   - セッションテーブルを作成（ユーザーIDを追加）

2. **ログイン機能**
   - ユーザー名とパスワードで認証
   - パスワードハッシュ化（bcrypt）
   - セッションIDを発行

3. **セッション管理**
   - セッションテーブルにユーザーIDを保存
   - セッションタイムアウト機能

4. **認証ミドルウェア**
   - セッションからユーザーIDを取得
   - セッション検証（既存の計画と同じ）

### フェーズ2: 管理機能（オプション）

1. **ユーザー管理画面**
   - ユーザー一覧表示
   - ユーザー追加
   - ユーザー編集
   - ユーザー削除（無効化）

2. **パスワード変更機能**
   - ログイン中のユーザーがパスワードを変更

3. **権限管理（将来の拡張）**
   - 管理者/一般ユーザーの権限分離

---

## 📊 実装の比較

### 単一ユーザー vs 複数ユーザー

| 項目 | 単一ユーザー | 複数ユーザー |
|------|------------|------------|
| **データベース設計** | セッションテーブルのみ | ユーザーテーブル + セッションテーブル |
| **ログイン機能** | 環境変数から読み込み | データベースから検索 |
| **パスワード管理** | 不要 | bcryptでハッシュ化 |
| **管理画面** | 不要 | オプション（後から追加可能） |
| **実装工数** | 小 | 中（管理画面なし） / 大（管理画面あり） |
| **難易度** | ⭐☆☆☆☆ | ⭐⭐☆☆☆（管理画面なし） |

---

## ✅ 実装チェックリスト（複数ユーザー対応版）

### フェーズ1: 基本機能（必須）

- [ ] ユーザーテーブルを作成
- [ ] セッションテーブルにユーザーIDを追加
- [ ] bcryptを追加（`requirements.txt`）
- [ ] パスワードハッシュ化機能を実装
- [ ] パスワード検証機能を実装
- [ ] ログイン機能を修正（データベースからユーザーを検索）
- [ ] セッション管理機能を修正（ユーザーIDを保存）
- [ ] 初期ユーザーの作成（マイグレーションスクリプト）

### フェーズ2: 管理機能（オプション）

- [ ] ユーザー管理画面を作成
- [ ] ユーザー追加機能を実装
- [ ] ユーザー編集機能を実装
- [ ] ユーザー削除機能を実装（無効化）
- [ ] パスワード変更機能を実装

---

## 🔧 実装の詳細

### データベース設計

```sql
-- ユーザーテーブル
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT 1
);

-- セッションテーブル（ユーザーIDを追加）
CREATE TABLE sessions (
    session_id TEXT PRIMARY KEY,
    user_id INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### パスワードハッシュ化

```python
import bcrypt

def hash_password(password: str) -> str:
    """パスワードをハッシュ化"""
    salt = bcrypt.gensalt()
    return bcrypt.hashpw(password.encode('utf-8'), salt).decode('utf-8')

def verify_password(password: str, password_hash: str) -> bool:
    """パスワードを検証"""
    return bcrypt.checkpw(password.encode('utf-8'), password_hash.encode('utf-8'))
```

### ログイン機能

```python
@app.route('/login', methods=['GET', 'POST'])
def login():
    if request.method == 'POST':
        username = request.form.get('username')
        password = request.form.get('password')
        
        # データベースからユーザーを検索
        user = get_user_by_username(username)
        
        if user and verify_password(password, user['password_hash']):
            # セッションIDを発行
            session_id = create_session(user['id'])
            response = redirect(url_for('dashboard'))
            response.set_cookie('session_id', session_id, 
                             secure=True, 
                             samesite='None',
                             httponly=True,
                             max_age=1800)  # 30分
            return response
        else:
            return render_template('login.html', error='ユーザー名またはパスワードが正しくありません')
    
    return render_template('login.html')
```

---

## 📝 初期ユーザーの作成

### 方法1: マイグレーションスクリプト（推奨）

```python
# scripts/create_initial_user.py
import bcrypt
import sqlite3

def create_initial_user(username, password):
    """初期ユーザーを作成"""
    password_hash = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt()).decode('utf-8')
    
    conn = sqlite3.connect('/home/AdminUser/nas-project-data/nas-dashboard/auth.db')
    cursor = conn.cursor()
    
    cursor.execute('''
        INSERT INTO users (username, password_hash) 
        VALUES (?, ?)
    ''', (username, password_hash))
    
    conn.commit()
    conn.close()
```

### 方法2: 環境変数から初期ユーザーを作成

```python
# 起動時に環境変数から初期ユーザーを作成（存在しない場合のみ）
if not user_exists(os.getenv('DASHBOARD_USERNAME')):
    create_user(
        os.getenv('DASHBOARD_USERNAME'),
        os.getenv('DASHBOARD_PASSWORD')
    )
```

---

## ⚠️ 注意事項

### パスワード管理

- パスワードは必ずハッシュ化して保存
- 平文で保存しない
- bcryptを使用（推奨）

### セキュリティ

- SQLインジェクション対策（パラメータ化クエリを使用）
- XSS対策（テンプレートエスケープ）
- CSRF対策（Flask-WTFを使用）

### データベースの場所

- 共有SQLiteデータベース: `/home/AdminUser/nas-project-data/nas-dashboard/auth.db`
- ユーザーテーブルとセッションテーブルを同じデータベースに保存

---

## 🎯 推奨実装方針

### 複数ユーザー対応を推奨する理由

1. **将来の拡張性**
   - 後から複数ユーザー対応が必要になった場合、大きな変更が必要
   - 最初から複数ユーザー対応にしておくと、将来的な変更が少ない

2. **実装コスト**
   - 単一ユーザーと複数ユーザーの実装コストの差は小さい
   - データベース設計とログイン機能の変更のみ

3. **柔軟性**
   - 家族やチームメンバーが利用する場合に対応可能
   - ユーザーごとに権限を設定できる（将来の拡張）

### 実装の段階的アプローチ

1. **フェーズ1: 基本機能のみ**
   - ユーザーテーブルとセッションテーブルを作成
   - ログイン機能を実装
   - 管理画面は後から追加可能

2. **フェーズ2: 管理機能（必要に応じて）**
   - ユーザー管理画面を追加
   - パスワード変更機能を追加

---

## ✅ 結論

**複数ユーザー対応は難しくありません**。

- **実装コスト**: 単一ユーザーと比べて少し増える程度
- **難易度**: ⭐⭐☆☆☆（やや簡単）
- **推奨**: 最初から複数ユーザー対応で実装することを推奨

---

**作成日**: 2025-11-04  
**更新日**: 2025-11-04  
**作成者**: AI Assistant

